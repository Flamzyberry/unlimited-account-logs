import {redirect} from 'next/navigation';
import {createClient} from '@/lib/supabase/server';
export default async function AdminPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user) redirect('/admin/login');const {data:profile}=await supabase.from('profiles').select('role,email,full_name').eq('id',user.id).single();if(profile?.role!=='admin') redirect('/admin/login');const {count}=await supabase.from('products').select('*',{count:'exact',head:true});return <main className="container" style={{padding:'40px 0'}}><div className="nav"><div><div className="brand">Unlimited Digital Admin</div><div style={{color:'#6b7280',fontSize:14}}>{profile.email}</div></div><form action="/auth/signout" method="post"><button className="btn secondary">Sign out</button></form></div><section className="section"><div className="grid"><div className="card"><h3>Products</h3><div className="price">{count??0}</div><p>Active marketplace products.</p></div><div className="card"><h3>Customers</h3><p>Customer management will be connected next.</p></div><div className="card"><h3>Orders</h3><p>Orders, payments and history will be connected next.</p></div></div></section></main>}